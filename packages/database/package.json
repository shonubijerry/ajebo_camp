{
  "name": "@ajebo_camp/database",
  "version": "0.1.0",
  "private": true,
  "main": "src/index.ts",
  "type": "module",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "npm run migration:apply && npm run prisma:generate && wrangler dev --persist-to .local_db",
    "start": "wrangler dev --persist-to .local_db",
    "prisma:generate": "prisma generate",
    "migration:create": "wrangler d1 migrations create dev-db-api $NAME --env dev",
    "migration:list": "wrangler d1 migrations list dev-db-api --env dev",
    "migration:apply": "echo y | wrangler d1 migrations apply dev-db-api --persist-to .local_db --local",
    "migrate:prisma": "npx prisma migrate diff --from-empty --to-schema-datamodel ./prisma/schema.prisma --script --output",
    "test": "vitest",
    "cf-typegen": "wrangler types"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.7.5",
    "@cloudflare/workers-types": "^4.20250421.0",
    "@ajebo_camp/shared": "*",
    "prisma": "^6.6.0",
    "prisma-json-types-generator": "^3.2.3",
    "typescript": "^5.5.2",
    "vitest": "~3.0.7",
    "wrangler": "^4.12.0"
  },
  "dependencies": {
    "@prisma/adapter-d1": "^6.6.0",
    "@prisma/client": "^6.6.0",
    "zod": "^3.22.3"
  }
}
